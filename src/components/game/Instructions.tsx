import React from 'react';

const Instructions: React.FC = () => {
    return (
        <div style={{ marginTop: '15px', color: '#555', fontSize: '16px' }}>
            Use <b>Arrow Up/Down</b> to aim. Hold <b>Spacebar</b> to charge and release to throw.
        </div>
    );
};

export default Instructions;
